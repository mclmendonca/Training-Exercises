#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, n;


    printf("Quantos elementos vai ter o vetor? ");
    scanf("%d", &n);

    double vet[n], soma = 0.0, cont = 0.0;

    for(i = 0; i < n; i++) {
       printf("Digite um numero: ");
       scanf("%lf", &vet[i]);
       soma = soma + vet[i]
       cont =

    }



    return 0;
}
